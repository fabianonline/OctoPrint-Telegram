### TODO

	# Aufräumen
		gettext("...")
		css
	  ! readme
	  	code doku

	# Commands
		delete/download files
		File List mehrseitig
		command for seeing user settings
	  ! update help
		settings granular

	# Features
		check webcam integration
		command states für user speichern
		markup option for send_msg()
		markup settings (html, markup) individual for notification messages
		tab page mit einstellungen für aktuellen print
		add stettings per user?
			-delay
			-message
			-icon etc.
		let user add custom masseges on events
			-custom Gcode commands
			-custom switches
		more icons (complete?)
		admin user
		send video
	    bei "left_chat_member" nicht löschen sondern deaktivieren
	  ! gEmo() auch aus message texten (.format(**locals())

	# Known Bugs (more to come :D)

	  ! on message migrate look for binding/ no setting changes

		update issues

		messages -> no_settings -> immer senden wenn keine chat_id in send_msg()

##### Done

		plugin stops if initial connect fails

		migrate
			- current < 1: update chats