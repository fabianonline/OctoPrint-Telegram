### TODO

	# Aufräumen
		gettext("...")
		css
	  ! readme
	  	code doku

	# Commands
		delete/download files
		File List mehrseitig
		command for seeing user settings
	  ! update help
		settings granular

	# Features
		cmd-messages in html markup?
		tab page mit einstellungen für aktuellen print
		add stettings per user?
			-delay
			-message
			-icon etc.
		let user add custom masseges on events
			-custom Gcode commands
			-custom switches
		more icons (complete?)
		admin user
		send video
		markup in notifications messages
	    bei "left_chat_member" nicht löschen sondern deaktivieren

	# Design
	  ! allow_users in group settings

	# Known Bugs (more to come :D)
		migrate
			- current < 1: update chats
			- current < target: update commands, notifications and messages